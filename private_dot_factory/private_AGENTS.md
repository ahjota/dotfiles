# Personal Agent Instructions

## Go Language Server (gopls) MCP

When the gopls MCP tools are available (you'll see `gopls___go_*` entries
in your tool list) and the task involves Go code, read
`~/.factory/docs/gopls-mcp.md` for usage guidance before using them.
