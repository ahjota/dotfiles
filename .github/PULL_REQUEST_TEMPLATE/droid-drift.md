<!-- droid-drift.md PR template for the droid-drift-monitor workflow.

  The workflow copies this file, then substitutes the HTML-comment
  placeholders with live values via sed before passing the result to
  `gh pr create --body-file`. The comments are invisible if left
  unreplaced, so the template is also safe to preview in the GitHub UI.

  Placeholders:
    <!-- DROID_WARNING -->  → hard-fail warning block (or removed)

    <!-- DRIFT_DATE -->     → run date (e.g. 2026-08-24)
    <!-- DROID_SUMMARY -->  → droid exec result text (multi-line)

-->

## Automated dependency/version drift report

<!-- DROID_WARNING -->

This PR was generated by the `droid-drift-monitor` GitHub Action (Meter
Reader Minion), which runs `droid exec` headlessly to scan for outdated
version/model pins across the repo.

Drift scan date: <!-- DRIFT_DATE -->

Refs #135

### Droid exec summary

<!-- DROID_SUMMARY -->
