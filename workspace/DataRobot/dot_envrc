# working in DataRobot always needs this
PYTHONPATH=$(pwd)
export PYTHONPATH

type workon > /dev/null || echo "workon not found, please install virtualenvwrapper"

# set virtualenv to the latest DataRobot
# TODO this doesn't actually work, I need to use workon or maybe something in QUANTUM
# export VIRTUAL_ENV=~/.virtualenvs/datarobot
