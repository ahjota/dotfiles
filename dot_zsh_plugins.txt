# .zsh_plugins.txt - comments begin with "#"

# This is used by antidote. Do you have it?
# ref: https://antidote.sh/
# Also, antidote pins everything. Did you update recently?

# Basic Zsh plugins are defined in user/repo format

# delays compinit until fpath is set.
# don't delete this again.
mattmc3/ez-compinit

# extra completion definitions (docker, aws, brew, etc.).
zsh-users/zsh-completions

# suggest commands from history as you type.
zsh-users/zsh-autosuggestions kind:defer

# fast syntax highlighting for commands.
zdharma-continuum/fast-syntax-highlighting kind:defer

# frecent directory jumping (z command).
agkozak/zsh-z

# replace aliases with abbreviations.
#olets/zsh-abbr kind:defer

# clear history when you go $HOME.
givensuman/zsh-allclear

# extract any archive with one command (x).
ohmyzsh/ohmyzsh path:plugins/extract

# typing ... expands to ../.., .... to ../../.., etc.
# ahjota fork: twang817 load-order fixes + vi-mode chunk delete.
ahjota/zsh-manydots-magic

# uv auto-activate venvs in cwd.
matthiasha/zsh-uv-env kind:defer

# auto-switch node.
# gretky/n.zsh kind:defer
# Disabled 2026-08-08: upstream's switch_n chpwd hook calls `exit 1` when
# `n` isn't installed, which kills the shell on every cd. The conditional
# loader in ~/.zshrc was removed in #88; a local rewrite sits unpublished
# at ~/workspace/n.zsh.
